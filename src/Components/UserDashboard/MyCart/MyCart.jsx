import React from 'react';
import Title from '../../../Hooks/Title';

const MyCart = () => {
    Title('My Cart')
    return (
        <div>
            
        </div>
    );
};

export default MyCart;